#include <stdio.h>

	int main () {
	int fat, n;
	
	fat = 1;
	
	printf ("Insira um numero: ");
	scanf ("%d", &n);
	
	while (n >= 1) {
		fat = fat * n;
		n--;
	}
	
	printf ("o fatorial de %d é %d",n, fat);
	
	return 0;	
	}
