#include <stdio.h>

	int main () {
	int fat, n;
	
	fat = 1;
	
	printf ("Insira um numero: ");
	scanf ("%d", &n);
	
	while (n >= 1) {
		fat = fat * n;
		n--;
	}
	
	printf ("%d", fat);
	
	return 0;	
	}
